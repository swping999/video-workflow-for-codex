interface:
  display_name: "Video Workflow"
  short_description: "Build synchronized videos from locked scripts"
  default_prompt: "Use $video-workflow to turn this script into a verified vertical video."
policy:
  allow_implicit_invocation: true
