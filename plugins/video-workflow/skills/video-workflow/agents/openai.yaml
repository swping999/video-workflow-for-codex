interface:
  display_name: "Video Workflow"
  short_description: "Turn one sentence into a free, verified video"
  default_prompt: "Use $video-workflow to make a vertical explainer video about MCP."
policy:
  allow_implicit_invocation: true
